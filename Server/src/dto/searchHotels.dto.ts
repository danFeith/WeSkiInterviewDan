export type SearchHotelSettings = {
    ski_site: number,
    from_date: string,
    to_date: string,
    min_group_size: number, 
    max_group_size: number
}

export type ClientSearchHotelSettings = {
    ski_site: number,
    from_date: string,
    to_date: string,
    group_size: number 
}